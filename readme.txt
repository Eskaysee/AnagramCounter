Type make into the terminal from the currenct directory and the program will compile & run.
To create javadocs type "make doc" and press enter to create java docs.
"make clean" to clear docs & bin directory (delete javadocs and binary/class files)
